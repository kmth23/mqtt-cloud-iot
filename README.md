# mqtt-cloud-iot
```
git clone https://github.com/kmth23/mqtt-cloud-iot.git
cd mqtt-cloud-iot
npm install
git clone https://github.com/kmth23/pythin-grovepi.git
node script.js --projectId=<<project-id>> --registryId=<<registry-id>> --deviceId=<<device-id>> --privateKeyFile=<<path/to/rsa_private.pem>> --algorithm=RS256 --cloudRegion=<<region>>
```
